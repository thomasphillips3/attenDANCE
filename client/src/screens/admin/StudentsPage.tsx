/**
 * StudentsPage -- placeholder for Plan 02-02 (Student & Family CRUD).
 * Will be replaced with full student list + search + filters.
 */
export default function StudentsPage() {
  return (
    <div style={{ fontFamily: 'var(--font-body)', fontSize: 18, color: 'var(--color-ink-2)' }}>
      Students
    </div>
  )
}
