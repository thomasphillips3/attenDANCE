/**
 * ClassDetail -- placeholder for Plan 02-03 (Class Management CRUD).
 * Will be replaced with class detail view (roster, schedule, enrollment).
 */
export default function ClassDetail() {
  return (
    <div style={{ fontFamily: 'var(--font-body)', fontSize: 18, color: 'var(--color-ink-2)' }}>
      Class Detail
    </div>
  )
}
