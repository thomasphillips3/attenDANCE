/**
 * ClassesPage -- placeholder for Plan 02-03 (Class Management CRUD).
 * Will be replaced with full class list + schedule view.
 */
export default function ClassesPage() {
  return (
    <div style={{ fontFamily: 'var(--font-body)', fontSize: 18, color: 'var(--color-ink-2)' }}>
      Classes
    </div>
  )
}
