# Plan 03-04: Payment History UI + Billing Integration Verification

## Goal
Admin can view a family's complete payment history and billing status. All billing flows work end-to-end: tuition plan to invoice to payment (manual or Stripe) to history visible.

## Requirements
- BILL-05: Payment history by family (paid, pending, overdue, waived)
- All BILL requirements verified end-to-end

## Dependencies
- Plan 03-03 (webhooks + recurring billing)

## Wave: 3

## Tasks

### T-03-04-01: Family billing detail page
**Files:**
- `client/src/screens/admin/FamilyBilling.tsx` — new screen, family's billing overview
- `client/src/router.tsx` — add /admin/families/:id/billing route
- `client/src/screens/admin/FamiliesPage.tsx` — add "Billing" action to family list rows

**Details:**
- FamilyBilling page shows:
  - Family name + Stripe customer status (linked / not linked)
  - Active enrollments with tuition plan amounts
  - Applicable discounts and net monthly cost
  - Invoice history table (date, amount, status, payment method)
  - Payment history table (date, amount, method, notes)
  - "Record Payment" and "Generate Invoice" action buttons
- Reuse existing hooks (useInvoices, usePayments from 03-02)
- Add useFamilyBilling hook that combines family + invoices + payments in one query

### T-03-04-02: Billing overview on admin billing page
**Files:**
- `client/src/screens/admin/BillingPage.tsx` — add overview section

**Details:**
- Add summary cards at top of BillingPage:
  - Total outstanding (sum of pending + overdue invoices)
  - Collected this month (sum of payments this month)
  - Overdue count (number of overdue invoices)
  - Active subscriptions count
- Add "Recent Payments" table showing last 10 payments across all families
- Link family names to /admin/families/:id/billing

### T-03-04-03: Integration verification checkpoint
**Details:**
- Verify all BILL requirements are met:
  - BILL-01: Tuition plan CRUD works for monthly/per_session/seasonal
  - BILL-02: Invoice generation calculates correct amount from enrollments + discounts
  - BILL-03: Stripe subscription creation works for monthly plans
  - BILL-04: All three discount types (sibling, scholarship, staff) apply correctly
  - BILL-05: Family billing page shows complete payment history
  - BILL-06: Cash + check payments recorded and visible
  - BILL-07: Failed payments tracked as overdue, Stripe retries configured
  - BILL-08: Webhook idempotency verified (duplicate events don't double-process)
- Build verification: `npm run build` passes in both server/ and client/
- TypeScript: no type errors

## Verification
- [ ] /admin/families/:id/billing shows family's complete billing overview
- [ ] Invoice history shows correct status badges
- [ ] Payment history shows all payment methods (stripe, cash, check)
- [ ] BillingPage overview cards show correct totals
- [ ] All BILL-01 through BILL-08 requirements satisfied
- [ ] `npm run build` passes in server/ and client/
