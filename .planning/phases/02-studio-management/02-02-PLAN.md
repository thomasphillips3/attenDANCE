---
phase: 02-studio-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/src/routes/students.ts
  - server/src/routes/families.ts
  - server/src/routes/rfid-cards.ts
  - server/src/index.ts
  - server/src/types/index.ts
  - client/src/screens/admin/StudentsPage.tsx
  - client/src/screens/admin/StudentForm.tsx
  - client/src/screens/admin/FamiliesPage.tsx
  - client/src/screens/admin/FamilyForm.tsx
  - client/src/hooks/useStudents.ts
  - client/src/hooks/useFamilies.ts
  - client/src/components/admin/PhotoUpload.tsx
  - client/src/components/admin/StudentSearch.tsx
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-03
  - STUD-04
  - STUD-05

must_haves:
  truths:
    - "Admin can create a student record with name, DOB, photo, medical notes, and skill level"
    - "Admin can edit and deactivate an existing student"
    - "Admin can create and edit family records with guardians, email, phone, emergency contact"
    - "Students belong to a family; family picker works in student form"
    - "Admin can assign an RFID card UID to a student"
    - "Admin can search students by name and filter by active status"
    - "Non-admin users get 403 on all student/family/RFID API endpoints"
  artifacts:
    - path: "server/src/routes/students.ts"
      provides: "GET /students, GET /students/:id, POST /students, PATCH /students/:id"
      exports: ["default"]
    - path: "server/src/routes/families.ts"
      provides: "GET /families, GET /families/:id, POST /families, PATCH /families/:id"
      exports: ["default"]
    - path: "server/src/routes/rfid-cards.ts"
      provides: "POST /rfid-cards, DELETE /rfid-cards/:id, GET /rfid-cards?studentId="
      exports: ["default"]
    - path: "client/src/screens/admin/StudentsPage.tsx"
      provides: "Student list with search bar and filter controls"
    - path: "client/src/screens/admin/StudentForm.tsx"
      provides: "Create/edit student form with photo upload and family picker"
    - path: "client/src/screens/admin/FamiliesPage.tsx"
      provides: "Family list page"
    - path: "client/src/screens/admin/FamilyForm.tsx"
      provides: "Create/edit family form"
    - path: "client/src/components/admin/PhotoUpload.tsx"
      provides: "Client-side image compression + Supabase Storage upload with signed URL display"
    - path: "client/src/components/admin/StudentSearch.tsx"
      provides: "Search input + active/inactive filter toggle"
  key_links:
    - from: "client/src/screens/admin/StudentsPage.tsx"
      to: "/students API"
      via: "useStudents hook with TanStack Query"
      pattern: "useQuery.*students"
    - from: "client/src/screens/admin/StudentForm.tsx"
      to: "/students API"
      via: "useMutation for create/update"
      pattern: "useMutation"
    - from: "client/src/components/admin/PhotoUpload.tsx"
      to: "Supabase Storage student-photos bucket"
      via: "supabase.storage.from('student-photos').upload"
    - from: "server/src/routes/students.ts"
      to: "Supabase Storage"
      via: "fastify.supabase.storage.from('student-photos').createSignedUrl for photo access"
---

<objective>
Build the full family and student management vertical slice: Fastify CRUD API routes for families, students, and RFID cards, plus the admin UI screens (StudentsPage with search/filter, StudentForm with photo upload and family picker, FamiliesPage, FamilyForm). After this plan, an admin can create families, add students to them with photos and medical notes, search/filter the student list, and assign RFID card UIDs.

Purpose: Students and families are the core data entities that feed class enrollment (Plan 02-04) and the attendance roster (Phase 1). This plan delivers the admin's ability to manage that data.
Output: 3 server route files, 4 admin screen components, 2 TanStack Query hooks, 2 admin UI components (PhotoUpload, StudentSearch).
</objective>

<execution_context>
@/Users/thomasphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thomasphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-studio-management/02-RESEARCH.md
@.planning/phases/02-studio-management/02-01-SUMMARY.md

<interfaces>
<!-- Existing Fastify route plugin pattern from sessions.ts -->
From server/src/routes/sessions.ts:
```typescript
import fp from 'fastify-plugin'
import type { FastifyPluginAsync } from 'fastify'

const sessionsRoutes: FastifyPluginAsync = async (fastify) => {
  // Routes defined here using fastify.get/post/patch
  // Always check: if (!organizationId) return reply.code(401)
  // Uses: fastify.supabase.from('table').select/insert/update
}

export default fp(sessionsRoutes, {
  name: 'sessions',
  dependencies: ['supabase', 'auth'],
})
```

From server/src/types/index.ts:
```typescript
import { Type, Static } from 'typebox'
// TypeBox schemas defined and exported here
// Pattern: const SchemaName = Type.Object({...})
//          type SchemaName = Static<typeof SchemaName>
```

From server/src/index.ts:
```typescript
// Registration pattern:
import studentsRoutes from './routes/students.js'
await fastify.register(studentsRoutes)
```

From client/src/hooks/useSessions.ts (TanStack Query pattern):
```typescript
const API_URL = import.meta.env.VITE_API_URL as string
// useQuery with queryKey, queryFn using fetch + Bearer token, enabled: !!token
// useMutation with mutationFn + onSuccess: queryClient.invalidateQueries
```

From client/src/hooks/useAuth.ts:
```typescript
export function useAuth(): {
  session: Session | null; user: User | null; loading: boolean;
  login: (email: string, password: string) => Promise<{ error: Error | null }>;
  logout: () => Promise<void>;
}
```

Existing DB schema (from 20260521000100_schema.sql):
```sql
-- students: id, organization_id, family_id, first_name, last_name, dob, photo_url, active, medical_notes, skill_level, created_at, updated_at
-- families: id, organization_id, primary_guardian_name, secondary_guardian_name, email, phone, emergency_contact_name, emergency_contact_phone, address, stripe_customer_id, created_at, updated_at
-- rfid_cards: id, organization_id, student_id, card_uid (varchar(64) UNIQUE), issued_at, active, created_at
```

Design tokens (from tokens.css):
```css
--color-purple: #8F2DB5;  --color-cream: #FAF7F2;
--color-white: #FFFFFF;    --color-ink: #1A1A2E;
--color-ink-2: #4A4A5E;   --color-ink-3: #8A8A9E;
--color-line: #E8E8EC;    --color-line-strong: #D0D0D8;
--color-purple-tint: #F3E8F9; --color-purple-tint-strong: #D8B4E8;
--font-display: 'DM Serif Display', serif;
--font-body: 'Atkinson Hyperlegible', sans-serif;
--radius-md: 12px;  --radius-sm: 8px;
/* Tap targets: 56px min. Body text: 18px min. */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fastify CRUD Routes -- Students, Families, RFID Cards</name>
  <files>
    server/src/routes/students.ts,
    server/src/routes/families.ts,
    server/src/routes/rfid-cards.ts,
    server/src/index.ts,
    server/src/types/index.ts
  </files>
  <action>
    Add TypeBox schemas to server/src/types/index.ts for all new endpoints. Follow the existing pattern (Type.Object with Static type alias). Schemas needed:
    - CreateStudentBody: familyId (string format uuid), firstName (string minLength 1), lastName (string minLength 1), dob (optional string format date), photoUrl (optional string), medicalNotes (optional string), skillLevel (optional string)
    - UpdateStudentBody: same fields as Create but all optional, plus active (optional boolean)
    - StudentListQuery: search (optional string), classId (optional string format uuid), active (optional boolean), page (optional number default 1), limit (optional number default 50 max 100)
    - CreateFamilyBody: primaryGuardianName (string minLength 1), email (string), phone (optional string), secondaryGuardianName (optional string), emergencyContactName (optional string), emergencyContactPhone (optional string), address (optional string)
    - UpdateFamilyBody: same as CreateFamily but all optional
    - CreateRfidCardBody: studentId (string format uuid), cardUid (string minLength 1)

    Create server/src/routes/students.ts following the Fastify plugin pattern from sessions.ts:
    - Every handler starts with: organizationId check (401 if missing), role check -- if (request.role !== 'admin') return reply.code(403).send({ error: 'Admin role required' })
    - GET /students: CHILDREN'S DATA — do NOT return medical_notes in the list endpoint. Use explicit column selection: query fastify.supabase.from('students').select('id, organization_id, family_id, first_name, last_name, dob, photo_url, active, skill_level, created_at, updated_at, families(primary_guardian_name, email)', { count: 'exact' }) filtered by .eq('organization_id', organizationId). Apply search filter: SANITIZE the search input first — strip characters that could break PostgREST filter syntax: const sanitized = search.replace(/[%_\\()"',]/g, ''). Then apply .or(`first_name.ilike.%${sanitized}%,last_name.ilike.%${sanitized}%`). Apply .eq('active', active) if active filter provided. CLASS FILTER (STUD-05): if classId query param is present, filter to students enrolled in that class: first query enrolled student IDs: const { data: enrolled } = await fastify.supabase.from('enrollments').select('student_id').eq('class_id', classId).eq('organization_id', organizationId).eq('status', 'active'). Then filter students: .in('id', enrolled.map(e => e.student_id)). If no enrolled students, return empty result immediately. Apply pagination with .range(from, from + limit - 1).order('last_name'). Return { data, total: count, page, limit }.
    - GET /students/:id: .select('*, families(id, primary_guardian_name, email, phone)').eq('id', params.id).eq('organization_id', organizationId).maybeSingle(). If not found return 404. If student has photo_url (a storage path, not a full URL), generate a signed URL: const { data: signedData } = await fastify.supabase.storage.from('student-photos').createSignedUrl(student.photo_url, 3600). Return student object with signedPhotoUrl field added. Also fetch RFID cards: fastify.supabase.from('rfid_cards').select('*').eq('student_id', params.id).eq('organization_id', organizationId).eq('active', true). Attach as rfidCards array on response.
    - POST /students: Validate familyId belongs to org: SELECT from families WHERE id = body.familyId AND organization_id. If not found, return 400 'Family not found in this organization'. Insert into students table with organization_id, snake_case field mapping (firstName -> first_name, etc.). Return 201 with created student (.select().single()).
    - PATCH /students/:id: Verify student belongs to org first (SELECT). Build update object from body fields that are present (skip undefined). If setting active=false, this is deactivation. Update with .update(changes).eq('id', params.id).eq('organization_id', organizationId).select().single(). Return updated student.

    Create server/src/routes/families.ts:
    - Same plugin pattern with fp(), name: 'families', dependencies: ['supabase', 'auth']
    - Same admin role gate on every handler
    - GET /families: .select('*, students(id)', { count: 'exact' }).eq('organization_id', organizationId).order('primary_guardian_name'). Map results to include studentCount: row.students?.length ?? 0. Paginate with page/limit from query. Return { data, total, page, limit }.
    - GET /families/:id: .select('*, students(id, first_name, last_name, active, photo_url)').eq('id', params.id).eq('organization_id', organizationId).maybeSingle(). 404 if not found. Return family with students array.
    - POST /families: insert with organization_id + snake_case mapped fields. Return 201.
    - PATCH /families/:id: verify org ownership, update present fields. Return updated.

    Create server/src/routes/rfid-cards.ts:
    - Same plugin pattern, name: 'rfid-cards', dependencies: ['supabase', 'auth']
    - Same admin role gate
    - GET /rfid-cards: require studentId query param. Select from rfid_cards where student_id = studentId AND organization_id AND active = true. Return array.
    - POST /rfid-cards: body has studentId and cardUid. Verify student belongs to org. Insert into rfid_cards with organization_id. card_uid has UNIQUE constraint; catch duplicate with: if error.code === '23505' return 409 { error: 'Card UID already assigned to another student' }. Return 201.
    - DELETE /rfid-cards/:id: soft delete -- UPDATE rfid_cards SET active = false WHERE id = params.id AND organization_id. If no row updated, return 404. Return 200 { message: 'Card deactivated' }.

    Register all three in server/src/index.ts:
    - Add imports for studentsRoutes, familiesRoutes, rfidCardsRoutes (from './routes/students.js', etc.)
    - Add await fastify.register() calls for each, after existing route registrations
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/LSODance\ Attendance/server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - GET/POST/PATCH /students with search, filter, pagination, signed photo URLs, RFID cards on detail
    - GET/POST/PATCH /families with student count on list, student array on detail
    - GET/POST/DELETE /rfid-cards with student ownership validation and duplicate UID protection
    - All endpoints gate on admin role and scope by organization_id from JWT
    - All new routes registered in index.ts
    - TypeBox schemas added to types/index.ts
    - Server TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin UI -- Student and Family Screens with Photo Upload and Search</name>
  <files>
    client/src/hooks/useStudents.ts,
    client/src/hooks/useFamilies.ts,
    client/src/components/admin/PhotoUpload.tsx,
    client/src/components/admin/StudentSearch.tsx,
    client/src/screens/admin/StudentsPage.tsx,
    client/src/screens/admin/StudentForm.tsx,
    client/src/screens/admin/FamiliesPage.tsx,
    client/src/screens/admin/FamilyForm.tsx
  </files>
  <action>
    Install browser-image-compression: cd client and npm install browser-image-compression@^2.0.2

    Create client/src/hooks/useStudents.ts:
    - Helper: getToken from useAuth().session?.access_token. API_URL from import.meta.env.VITE_API_URL.
    - useStudents(filters: { search?: string; active?: boolean; page?: number }): useQuery with queryKey ['students', filters]. queryFn builds URLSearchParams from filters, fetches GET {API_URL}/students?{params} with Authorization Bearer header. Returns typed { data: Student[], total: number, page: number, limit: number }. enabled: !!token.
    - useStudent(id: string | undefined): useQuery with queryKey ['students', id]. Fetches GET {API_URL}/students/{id}. Returns student with signedPhotoUrl and rfidCards array. enabled: !!token && !!id.
    - useCreateStudent(): useMutation. mutationFn POSTs JSON to {API_URL}/students. onSuccess: queryClient.invalidateQueries({ queryKey: ['students'] }).
    - useUpdateStudent(id: string): useMutation. mutationFn PATCHes JSON to {API_URL}/students/{id}. onSuccess: invalidate ['students'] and ['students', id].
    - Define Student interface matching API response shape (id, firstName as camelCase mapped from snake_case API response, etc.). Note: the Fastify API returns snake_case from Supabase; either map in the hook or keep snake_case in the frontend. Recommendation: keep snake_case to avoid mapping bugs -- use snake_case field names in the TypeScript interface (first_name, last_name, etc.) since that matches the API response directly.

    Create client/src/hooks/useFamilies.ts:
    - useFamilies(page?: number): useQuery for GET /families. Return { data: Family[], total, isLoading }.
    - useFamily(id: string | undefined): useQuery for GET /families/:id. Returns family with students array.
    - useCreateFamily(): useMutation POST /families, invalidate ['families'].
    - useUpdateFamily(id: string): useMutation PATCH /families/:id, invalidate ['families'] and ['families', id].

    Create client/src/components/admin/PhotoUpload.tsx:
    - Props: currentPhotoUrl?: string (signed URL for display), onUpload: (storagePath: string) => void, studentId?: string
    - Render: 96x96 circle. If currentPhotoUrl, show img with object-fit cover, border-radius 50%. Else show gray placeholder circle with a generic person SVG icon (simple inline SVG, no external dependency).
    - Below image: button "Upload Photo" (or "Change Photo" if currentPhotoUrl exists). Style: 48px height, font-size 14px, font-weight 600, border 1px solid var(--color-line-strong), border-radius var(--radius-sm), background white, color var(--color-ink-2). Min 56px tap target via padding.
    - Hidden file input (accept="image/jpeg,image/png,image/webp"), triggered by button click.
    - On file select: show "Uploading..." text on button, disable it. Import imageCompression from browser-image-compression. Compress: await imageCompression(file, { maxSizeMB: 0.2, maxWidthOrHeight: 400, useWebWorker: true }). Upload: import supabase from lib/supabase. CHILDREN'S DATA — path MUST include organization_id for storage RLS: get organizationId from JWT (decode session.access_token, read app_metadata.organization_id). const path = `${organizationId}/${studentId || 'new'}/${Date.now()}.jpg`. await supabase.storage.from('student-photos').upload(path, compressed, { contentType: 'image/jpeg', upsert: true }). On success: call onUpload(path). On error: show error text below button in red (var(--color-red, #D32F2F)), font-size 14px.
    - Props should also accept organizationId: string (passed from the form which reads it from the JWT).
    - Accessibility: aria-label="Upload student photo" on button, alt="Student photo" on img.

    Create client/src/components/admin/StudentSearch.tsx:
    - Props: search: string, onSearchChange: (val: string) => void, activeFilter: boolean | undefined, onActiveFilterChange: (val: boolean | undefined) => void, classFilter: string | undefined, onClassFilterChange: (val: string | undefined) => void, classes: Array<{ id: string; name: string }>
    - Search input: type="search", placeholder="Search students...", font-size 16px, height 48px, padding 12px 16px, border 1px solid var(--color-line), border-radius var(--radius-sm), font-family var(--font-body), width 100% max-width 360px. On change: debounce 300ms using useState + useEffect with setTimeout/clearTimeout, then call onSearchChange.
    - Filter buttons: three buttons in a row -- "All" (active=undefined), "Active" (active=true), "Inactive" (active=false). Selected button: background var(--color-purple), color white, font-weight 700. Unselected: background white, border 1px solid var(--color-line), color var(--color-ink-2). Each 44px height, padding 8px 16px, border-radius var(--radius-sm), font-size 14px, font-family var(--font-body). On click: call onActiveFilterChange with the appropriate value.
    - Class filter: native select dropdown. First option: "All Classes" (value=""). Then map props.classes to options with value={class.id} label={class.name}. Style: height 44px, font-size 14px, font-family var(--font-body), border 1px solid var(--color-line), border-radius var(--radius-sm), padding 8px 12px, background white. On change: call onClassFilterChange(value || undefined).
    - Layout: flex row, gap 12px, align-items center, flex-wrap wrap

    Replace client/src/screens/admin/StudentsPage.tsx (overwrite Plan 02-01 placeholder):
    - Default export (required for lazy loading in router.tsx)
    - Header row: flex, justify-content space-between, align-items center, margin-bottom 24px. Left: "Students" in font-family var(--font-display), font-size 32px, color var(--color-ink). Right: Link to /admin/students/new styled as button -- background var(--color-purple), color white, font-size 16px, font-weight 600, height 48px, padding 0 24px, border-radius var(--radius-sm), display inline-flex align-items center, text "Add Student", min 56px tap target.
    - Below header: StudentSearch component. State: search (string), activeFilter (boolean | undefined, default undefined = all), classFilter (string | undefined, default undefined = all), page (number, default 1). Also fetch class list for the dropdown: import useClasses from hooks (will exist after Plan 02-03 executes; if not yet available, conditionally import or pass empty array). Pass all filter state as props to StudentSearch.
    - Data: const { data, total, isLoading } = useStudents({ search, active: activeFilter, classId: classFilter, page }). Reset page to 1 when search, activeFilter, or classFilter changes.
    - Loading: centered spinner (same style as ClassList).
    - Empty state: "No students found" centered, font-size 18px, color var(--color-ink-3), font-style italic.
    - Student list: vertical stack with gap 8px. Each student is a Link to /admin/students/{student.id} styled as a card: background white, border 1px solid var(--color-line), border-radius var(--radius-md), padding 16px, display flex, align-items center, gap 16px, min-height 72px, text-decoration none, color inherit. Hover: border-color var(--color-purple-tint-strong). Inside: 40px circle photo (if photo_url show signed URL img, else gray placeholder), name block (first_name + last_name at 18px font-weight 600 color var(--color-ink), family primary_guardian_name at 14px color var(--color-ink-3)), active badge on right (green circle 8px for active, gray for inactive, with "Active"/"Inactive" text at 13px).
    - Pagination: if total > limit (50), show "Showing {from+1}-{Math.min(from+limit, total)} of {total}" at 14px, with "Previous" / "Next" buttons (56px height, font-size 14px, disabled when at bounds).

    Replace client/src/screens/admin/StudentForm.tsx:
    - Default export. Read :id from useParams(). const isEdit = !!id.
    - If isEdit: const { data: student, isLoading } = useStudent(id). Show spinner while loading.
    - Form with react-hook-form: const { register, handleSubmit, formState: { errors }, setValue, watch } = useForm({ resolver: zodResolver(studentSchema), defaultValues populated from student data in edit mode }). Use useEffect to reset form when student data loads.
    - Zod schema (define in-file): z.object({ first_name: z.string().min(1, 'First name required'), last_name: z.string().min(1, 'Last name required'), dob: z.string().optional().or(z.literal('')), medical_notes: z.string().optional(), skill_level: z.string().optional(), family_id: z.string().uuid('Please select a family') })
    - Title: "Add Student" or "Edit Student" in font-display 28px.
    - Form layout: max-width 600px. Each field: label (14px, font-weight 600, color var(--color-ink-2), margin-bottom 4px) + input (font-size 16px, height 48px, padding 12px 16px, border 1px solid var(--color-line), border-radius var(--radius-sm), width 100%, font-family var(--font-body)). Error text in red 13px below input. Fields gap 16px.
    - Fields: First Name (text), Last Name (text), Date of Birth (type="date"), Medical Notes (textarea, 3 rows), Skill Level (select with options: "", "Beginner", "Intermediate", "Advanced", "Pre-Professional").
    - Family picker: a native select element populated from useFamilies(). Shows primary_guardian_name for each family. If no families exist, show text "No families yet" with a Link to /admin/families/new ("Create a family first" in purple).
    - PhotoUpload component: placed above the form fields. Pass currentPhotoUrl={student?.signedPhotoUrl}, studentId={id}, onUpload={(path) => setValue('photo_url', path)}. Track photo_url in form state (hidden field).
    - RFID section: rendered only in edit mode, below the main form, separated by a horizontal rule. Heading "RFID Card" at 18px font-weight 600. If student.rfidCards has entries, show each card_uid in a row with a "Remove" button (calls DELETE /rfid-cards/:cardId then refetch student). Below: text input (placeholder "Enter card UID") + "Assign Card" button. On submit: POST /rfid-cards with { studentId: id, cardUid: inputValue }. On 409 error: show "Card already assigned" error. Refetch student on success.
    - Action buttons row: "Save Student" button (background var(--color-purple), color white, 48px height, font-size 16px, font-weight 600, border-radius var(--radius-sm), min-width 160px). "Cancel" button (background white, border 1px solid var(--color-line-strong), color var(--color-ink-2), same dimensions, navigates to /admin/students). In edit mode, add "Deactivate Student" button (background white, border 1px solid #D32F2F, color #D32F2F, same dimensions). Deactivate opens a Radix Dialog confirmation: "Deactivate {first_name} {last_name}? They will no longer appear in class rosters." Confirm button PATCHes active=false, then navigates to /admin/students.
    - On form submit: if isEdit, call useUpdateStudent(id).mutateAsync(data); else call useCreateStudent().mutateAsync(data). On success: navigate('/admin/students'). On error: show error message below the form.

    Replace client/src/screens/admin/FamiliesPage.tsx:
    - Default export. Header: "Families" (font-display 32px) + "Add Family" link-button to /admin/families/new (same purple button style).
    - Data: useFamilies(). Loading spinner. Empty state: "No families yet".
    - Family list: cards (same style as student cards). Each shows: primary_guardian_name (18px, font-weight 600), email (14px, var(--color-ink-3)), phone (14px, var(--color-ink-3)), student count badge ("3 students" in a pill: background var(--color-purple-tint), color var(--color-purple), font-size 13px, padding 2px 10px, border-radius 12px). Card links to /admin/families/:id.

    Replace client/src/screens/admin/FamilyForm.tsx:
    - Default export. Read :id from useParams(). isEdit = !!id.
    - If isEdit: useFamily(id) for data.
    - Zod schema: primary_guardian_name (string min 1), email (string email), phone (string optional), secondary_guardian_name (string optional), emergency_contact_name (string optional), emergency_contact_phone (string optional), address (string optional)
    - Title: "Add Family" or "Edit Family".
    - Form fields: Primary Guardian Name, Secondary Guardian Name, Email, Phone, Emergency Contact Name, Emergency Contact Phone, Address (textarea). Same input styling as StudentForm.
    - In edit mode: below form, show "Students in this Family" section with list of linked students (from family.students). Each shows first_name + last_name as a Link to /admin/students/:studentId. If no students: "No students linked to this family yet."
    - Save/Cancel buttons same style as StudentForm.
    - On submit: useCreateFamily or useUpdateFamily. Navigate to /admin/families on success.
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/LSODance\ Attendance/client && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - StudentsPage shows searchable, filterable list of students with photo thumbnails and family names
    - StudentForm creates/edits students with photo upload (compressed to 200KB, private bucket), family picker dropdown, and RFID card assignment section
    - FamiliesPage lists all families with student counts in pill badges
    - FamilyForm creates/edits families with all guardian and emergency contact fields, shows linked students in edit mode
    - Photo upload compresses images client-side before uploading to private Supabase Storage bucket
    - Search debounces 300ms, filter toggle switches All/Active/Inactive
    - All forms use react-hook-form + zod validation
    - All UI meets accessibility: 18px+ body text, 56px+ tap targets, Atkinson Hyperlegible font
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Fastify API | All CRUD operations require admin role; org scoping from JWT |
| Client -> Supabase Storage | Photo upload requires authenticated session to private bucket |
| API -> Database | Service role client bypasses RLS; org scoping enforced in query WHERE clauses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Elevation of Privilege | All CRUD endpoints | mitigate | Every handler checks request.role !== 'admin' and returns 403. Role from JWT app_metadata only. |
| T-02-06 | Information Disclosure | GET /students, GET /families | mitigate | Every query filters by request.organizationId from JWT. Cross-tenant access impossible. GET /students list excludes medical_notes — only returned on GET /students/:id detail endpoint. |
| T-02-07 | Tampering | POST /students familyId | mitigate | Verify familyId belongs to same organization before insert. Prevents cross-org family linking. |
| T-02-08 | Tampering | Photo upload | mitigate | Private bucket, file_size_limit=2MB, allowed_mime_types restricted. Client compresses. Server generates signed URLs (1hr expiry). Upload path includes organization_id for storage RLS scoping. |
| T-02-10 | Information Disclosure | Children's medical data | mitigate | medical_notes excluded from list endpoint (only on detail). Fastify request body logging MUST NOT include medical_notes — use fastify.addHook('onSend') or schema serialization to strip sensitive fields from logs. |
| T-02-11 | Tampering | Search input injection | mitigate | Search input sanitized — special characters (%_\\()"',) stripped before building PostgREST filter string. Prevents filter syntax manipulation. |
| T-02-09 | Spoofing | RFID card assignment | mitigate | card_uid has DB UNIQUE constraint. Duplicate returns 409. Student ownership verified against org. |
| T-02-SC | Tampering | npm install browser-image-compression | mitigate | Verified in RESEARCH.md audit: 7+ years, 1.5M/wk downloads. Approved. |
</threat_model>

<verification>
- Server compiles: cd server && npx tsc --noEmit
- Client compiles: cd client && npx tsc --noEmit
- Admin can navigate to /admin/students and see the student list
- Admin can create a new student with photo and family link
- Admin can search and filter the student list
- Admin can assign and remove RFID cards on student detail
- Admin can create and edit families
</verification>

<success_criteria>
- Admin can create a family, then create a student linked to that family with photo, DOB, medical notes, and skill level (STUD-01, STUD-02, STUD-03)
- Admin can assign an RFID card UID to a student (STUD-04)
- Admin can search students by name and filter by active/inactive status (STUD-05)
- All CRUD operations are admin-gated and org-scoped
- Photo upload uses client-side compression and private Supabase Storage bucket with signed URLs
</success_criteria>

<output>
Create `.planning/phases/02-studio-management/02-02-SUMMARY.md` when done
</output>
