---
phase: 02-studio-management
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/src/routes/classes.ts
  - server/src/index.ts
  - server/src/types/index.ts
  - client/src/screens/admin/ClassesPage.tsx
  - client/src/screens/admin/ClassForm.tsx
  - client/src/hooks/useClasses.ts
  - client/src/hooks/useStaff.ts
  - client/src/components/admin/WeeklyCalendar.tsx
autonomous: true
requirements:
  - CLAS-01
  - CLAS-02

must_haves:
  truths:
    - "Admin can create a class with name, type, instructor, day/time, duration, room, capacity, age range, and level"
    - "Admin can edit an existing class"
    - "Admin can view all classes on a visual weekly calendar showing day/time slots"
    - "Weekly calendar shows class name, instructor, and enrollment count per block"
    - "Non-admin users get 403 on class API endpoints"
  artifacts:
    - path: "server/src/routes/classes.ts"
      provides: "GET /classes, GET /classes/:id, POST /classes, PATCH /classes/:id"
      exports: ["default"]
    - path: "client/src/screens/admin/ClassesPage.tsx"
      provides: "Class list view and weekly calendar view with toggle"
    - path: "client/src/screens/admin/ClassForm.tsx"
      provides: "Create/edit class form with instructor picker"
    - path: "client/src/components/admin/WeeklyCalendar.tsx"
      provides: "CSS Grid weekly schedule with class blocks"
    - path: "client/src/hooks/useClasses.ts"
      provides: "TanStack Query hooks for class CRUD"
    - path: "client/src/hooks/useStaff.ts"
      provides: "TanStack Query hook for staff list (instructor picker)"
  key_links:
    - from: "client/src/screens/admin/ClassesPage.tsx"
      to: "/classes API"
      via: "useClasses hook"
      pattern: "useQuery.*classes"
    - from: "client/src/components/admin/WeeklyCalendar.tsx"
      to: "client/src/screens/admin/ClassesPage.tsx"
      via: "Receives classes array as prop"
    - from: "client/src/screens/admin/ClassForm.tsx"
      to: "client/src/hooks/useStaff.ts"
      via: "useStaff hook populates instructor dropdown"
---

<objective>
Build the class management vertical slice: Fastify CRUD API for classes, admin screens for class list with weekly calendar view, and class create/edit form with instructor picker. After this plan, an admin can create classes with all scheduling details and see them displayed on a visual weekly calendar.

Purpose: Classes are the scheduling backbone of the studio. The weekly calendar gives Carollette a bird's-eye view of the entire studio week. Class data feeds enrollment (Plan 02-04) and attendance sessions (Phase 1).
Output: 1 server route file, 2 admin screen components, 1 WeeklyCalendar component, 2 TanStack Query hooks.
</objective>

<execution_context>
@/Users/thomasphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thomasphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-studio-management/02-RESEARCH.md
@.planning/phases/02-studio-management/02-01-SUMMARY.md

<interfaces>
<!-- Fastify plugin pattern -->
From server/src/routes/sessions.ts:
```typescript
import fp from 'fastify-plugin'
import type { FastifyPluginAsync } from 'fastify'
const routes: FastifyPluginAsync = async (fastify) => { /* handlers */ }
export default fp(routes, { name: 'route-name', dependencies: ['supabase', 'auth'] })
```

From server/src/types/index.ts:
```typescript
import { Type, Static } from 'typebox'
```

Existing DB schema (from 20260521000100_schema.sql):
```sql
-- classes: id, organization_id, name, type (class_type enum), instructor_id (FK staff),
--   day_of_week (smallint 0-6), start_time (time), duration_minutes (smallint),
--   room, capacity (smallint), age_min, age_max, level, active, created_at, updated_at
-- class_type ENUM: 'recurring', 'drop_in', 'workshop'
-- staff: id, organization_id, user_id, first_name, last_name, email, role (staff_role enum), active
-- enrollments: id, organization_id, student_id, class_id, status (enrollment_status enum)
```

TanStack Query pattern (from client/src/hooks/useSessions.ts):
```typescript
// useQuery({ queryKey, queryFn: async () => fetch(url, { headers: { Authorization: `Bearer ${token}` } }), enabled: !!token })
// useMutation({ mutationFn, onSuccess: () => queryClient.invalidateQueries({ queryKey }) })
```

Design tokens:
```css
--color-purple: #8F2DB5; --color-cream: #FAF7F2; --color-white: #FFFFFF;
--color-ink: #1A1A2E; --color-ink-2: #4A4A5E; --color-ink-3: #8A8A9E;
--color-line: #E8E8EC; --color-purple-tint: #F3E8F9;
--font-display: 'DM Serif Display'; --font-body: 'Atkinson Hyperlegible';
--radius-md: 12px; --radius-sm: 8px;
```

WeeklyCalendar interface (from 02-RESEARCH.md):
```typescript
interface CalendarClass {
  id: string; name: string; dayOfWeek: number; startTime: string;
  durationMinutes: number; instructorName?: string; room?: string;
  enrolledCount: number; capacity: number | null;
}
// CSS Grid: gridTemplateColumns '60px repeat(7, 1fr)', rows for hours
// Class blocks positioned by day (column) and time (row)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fastify Classes CRUD Route + Staff List Endpoint</name>
  <files>
    server/src/routes/classes.ts,
    server/src/index.ts,
    server/src/types/index.ts
  </files>
  <action>
    Add TypeBox schemas to server/src/types/index.ts:
    - CreateClassBody: name (string minLength 1), type (union literal 'recurring'|'drop_in'|'workshop', default 'recurring'), instructorId (optional string format uuid), dayOfWeek (optional number minimum 0 maximum 6), startTime (string), durationMinutes (number minimum 1), room (optional string), capacity (optional number minimum 1), ageMin (optional number minimum 0), ageMax (optional number minimum 0), level (optional string)
    - UpdateClassBody: same fields all optional, plus active (optional boolean)
    - ClassListQuery: active (optional boolean)
    - StaffListItem response type not needed in TypeBox (server returns ad hoc)

    Create server/src/routes/classes.ts:
    - Fastify plugin with fp(), name: 'classes', dependencies: ['supabase', 'auth']
    - Admin role gate on every handler: if (request.role !== 'admin') return reply.code(403).send({ error: 'Admin role required' })
    - Organization check: if (!organizationId) return reply.code(401).send({ error: 'Missing organization context' })

    - GET /classes: query fastify.supabase.from('classes').select('*, staff:instructor_id(id, first_name, last_name)').eq('organization_id', organizationId). If active query param provided, filter .eq('active', active); default to active=true. Order by .order('day_of_week').order('start_time'). For enrollment counts, run a second query: fastify.supabase.from('enrollments').select('class_id').eq('organization_id', organizationId).eq('status', 'active'). Group by class_id in JavaScript to build a Map of classId -> count. Merge counts into class results. Return { data: classesWithCounts }.

    - GET /classes/:id: select class with staff join: .select('*, staff:instructor_id(id, first_name, last_name)').eq('id', params.id).eq('organization_id', organizationId).maybeSingle(). If not found, 404. Fetch enrollments for this class: .from('enrollments').select('id, status, student_id, students(id, first_name, last_name, active, photo_url)').eq('class_id', params.id).eq('organization_id', organizationId).order('enrolled_at'). Partition into activeEnrollments (status='active'), waitlistEnrollments (status='waitlist'). Compute enrolledCount and waitlistCount. Return class object with these fields attached.

    - POST /classes: if body.instructorId is provided, verify the staff member belongs to this org: fastify.supabase.from('staff').select('id').eq('id', body.instructorId).eq('organization_id', organizationId).maybeSingle(). If not found, return 400 'Instructor not found in this organization'. If body.ageMin and body.ageMax are both provided and ageMax < ageMin, return 400 'Maximum age must be >= minimum age'. Insert into classes with snake_case mapping: { organization_id: organizationId, name: body.name, type: body.type, instructor_id: body.instructorId || null, day_of_week: body.dayOfWeek ?? null, start_time: body.startTime, duration_minutes: body.durationMinutes, room: body.room || null, capacity: body.capacity || null, age_min: body.ageMin ?? null, age_max: body.ageMax ?? null, level: body.level || null }. Return 201 with .select().single().

    - PATCH /classes/:id: verify class belongs to org (select, check exists). Build update object from present body fields with snake_case mapping. fastify.supabase.from('classes').update(changes).eq('id', params.id).eq('organization_id', organizationId).select().single(). Return updated.

    Add GET /staff endpoint. Two options -- since staff management is Phase 5, add it as a minimal route within classes.ts or create a thin server/src/routes/staff-list.ts. Preferred: add to classes.ts as a co-located route since it is only needed for the instructor picker.
    - GET /staff: admin role gate. fastify.supabase.from('staff').select('id, first_name, last_name, role').eq('organization_id', organizationId).eq('active', true).order('last_name'). Return data array directly.

    Register classesRoutes in server/src/index.ts: import classesRoutes from './routes/classes.js' and await fastify.register(classesRoutes).
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/LSODance\ Attendance/server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - GET /classes returns class list with instructor names and enrollment counts
    - GET /classes/:id returns class detail with enrollment breakdown (active, waitlist)
    - POST /classes creates a class with scheduling details, validates instructor org membership
    - PATCH /classes/:id updates a class
    - GET /staff returns active staff for instructor picker
    - All endpoints admin-gated and org-scoped
    - Server compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin UI -- ClassesPage with WeeklyCalendar + ClassForm with Instructor Picker</name>
  <files>
    client/src/hooks/useClasses.ts,
    client/src/hooks/useStaff.ts,
    client/src/components/admin/WeeklyCalendar.tsx,
    client/src/screens/admin/ClassesPage.tsx,
    client/src/screens/admin/ClassForm.tsx
  </files>
  <action>
    Create client/src/hooks/useClasses.ts:
    - useClasses(): useQuery with queryKey ['classes']. Fetch GET {API_URL}/classes with Bearer token. Return { data: ClassItem[], isLoading }.
    - useClass(id: string | undefined): useQuery with queryKey ['classes', id]. Fetch GET {API_URL}/classes/{id}. Returns class detail with activeEnrollments, waitlistEnrollments, enrolledCount, waitlistCount. enabled: !!token && !!id.
    - useCreateClass(): useMutation POST /classes with JSON body. onSuccess: invalidate ['classes'].
    - useUpdateClass(id: string): useMutation PATCH /classes/{id}. onSuccess: invalidate ['classes'] and ['classes', id].
    - Define ClassItem interface matching API response: id, name, type, instructor_id, day_of_week, start_time, duration_minutes, room, capacity, age_min, age_max, level, active, staff (nested { id, first_name, last_name } or null), enrolledCount.

    Create client/src/hooks/useStaff.ts:
    - useStaff(): useQuery with queryKey ['staff']. Fetch GET {API_URL}/staff with Bearer token. Return { data: StaffMember[], isLoading }. StaffMember interface: { id: string, first_name: string, last_name: string, role: string }.

    Create client/src/components/admin/WeeklyCalendar.tsx:
    - Props: classes: CalendarClass[], onClassClick?: (classId: string) => void
    - CalendarClass interface (define in this file): id, name, dayOfWeek (number 0-6), startTime (string "HH:MM"), durationMinutes (number), instructorName (string optional), room (string optional), enrolledCount (number), capacity (number | null)
    - Constants: DAYS = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat']. START_HOUR = 7. END_HOUR = 20. HOURS = Array.from({ length: END_HOUR - START_HOUR }, (_, i) => i + START_HOUR) -- covers 7AM to 7PM (13 rows).
    - Container: CSS Grid. display grid, gridTemplateColumns '60px repeat(7, 1fr)', gridTemplateRows '40px repeat(13, 60px)'. border 1px solid var(--color-line), border-radius var(--radius-md), overflow hidden, background white, position relative.
    - Header row (gridRow 1): empty cell at column 1, then day labels in columns 2-8. Each: padding 8px, text-align center, font-weight 700, font-size 13px, font-family var(--font-body), border-bottom 1px solid var(--color-line), background var(--color-cream).
    - Time label column (gridColumn 1, rows 2-14): each hour. padding 4px 8px, font-size 12px, color var(--color-ink-3), border-right 1px solid var(--color-line), border-bottom 1px solid var(--color-line). Format: hour > 12 ? (hour-12)+'p' : hour === 12 ? '12p' : hour+'a'.
    - Grid background lines: for visual grid lines, render empty divs for each cell (row 2-14, columns 2-8) with border-bottom and border-right 1px solid var(--color-line), opacity 0.5. Or use a simpler approach: set background on the container and use gap with background-color showing through.
    - Class blocks: for each class in the classes array that has a dayOfWeek (skip null -- those are drop-in/workshop), compute position. Parse startTime: const [h, m] = startTime.split(':').map(Number). gridColumn = dayOfWeek + 2 (1-indexed, offset for time label). For sub-hour precision: gridRow start = (h - START_HOUR) + 2. Duration in rows: use style for height rather than grid span for precision. Set the block to position absolute within a relatively-positioned grid area, or use a simpler approach: gridRow = `${(h - START_HOUR) + 2} / span ${Math.max(1, Math.ceil(durationMinutes / 60))}`. The simpler approach (span by hours rounded up) is acceptable for MVP -- most dance classes are 45-60 min.
    - Block style: background var(--color-purple-tint), border-left 3px solid var(--color-purple), border-radius 6px, padding 4px 8px, margin 2px, font-size 12px, overflow hidden, cursor pointer, min-height 0. Hover: background var(--color-purple-tint-strong), transition background 150ms.
    - Block content: div with class name (font-weight 700, color var(--color-ink), white-space nowrap, overflow hidden, text-overflow ellipsis), div with instructorName (color var(--color-ink-3)), div with enrolledCount + '/' + (capacity ?? '--') (color var(--color-ink-3)).
    - onClick: call onClassClick(cls.id).
    - Accessibility: each block has role="button", tabIndex=0, aria-label="{name}, {dayLabel} {formattedTime}, {enrolledCount} of {capacity} enrolled". onKeyDown: Enter or Space triggers click.

    Replace client/src/screens/admin/ClassesPage.tsx (overwrite Plan 02-01 placeholder):
    - Default export.
    - State: viewMode ('calendar' | 'list'), default 'calendar'.
    - Header row: "Classes" (font-display 32px) on left. Right side: view toggle + Add Class button.
    - View toggle: two buttons side by side. "Calendar" and "List". Active: background var(--color-purple), color white, font-weight 700. Inactive: background white, border 1px solid var(--color-line), color var(--color-ink-2). Each 40px height, padding 8px 16px, font-size 14px. First has border-radius '8px 0 0 8px', second '0 8px 8px 0'. No gap between them (connected toggle).
    - Add Class: Link to /admin/classes/new, same purple button style as StudentsPage (48px height, etc.).
    - Data: const { data: classes, isLoading } = useClasses(). Loading: spinner. Empty: "No classes yet" centered italic.
    - Calendar view (viewMode === 'calendar'): Map classes to CalendarClass shape: { id: cls.id, name: cls.name, dayOfWeek: cls.day_of_week, startTime: cls.start_time, durationMinutes: cls.duration_minutes, instructorName: cls.staff ? cls.staff.first_name + ' ' + cls.staff.last_name : undefined, room: cls.room, enrolledCount: cls.enrolledCount ?? 0, capacity: cls.capacity }. Render WeeklyCalendar with onClassClick={(id) => navigate('/admin/classes/' + id)}.
    - List view (viewMode === 'list'): vertical card list, gap 8px. Each class card: Link to /admin/classes/{cls.id}, styled as card (same as StudentsPage cards). Content: class name (18px font-weight 600), schedule line ("Mon 4:00 PM - 5:00 PM" computed from day_of_week + start_time + duration_minutes, at 14px color var(--color-ink-2)), instructor name (14px var(--color-ink-3)), enrollment pill (enrolledCount + '/' + (capacity ?? 'Open') in background var(--color-purple-tint), color var(--color-purple), font-size 13px, padding 2px 10px, border-radius 12px). Inactive classes: opacity 0.5, "(Inactive)" badge.
    - Format time helper: convert "16:00" to "4:00 PM". Format day: ['Sunday','Monday',...][day_of_week]. Format end time: add duration_minutes to start_time.

    Replace client/src/screens/admin/ClassForm.tsx (overwrite Plan 02-01 placeholder):
    - Default export. const { id } = useParams(). const isEdit = !!id.
    - If isEdit: const { data: classData, isLoading } = useClass(id). Show spinner while loading.
    - Zod schema (define in file): z.object({ name: z.string().min(1, 'Class name is required'), type: z.enum(['recurring','drop_in','workshop']), instructor_id: z.string().uuid().optional().or(z.literal('')), day_of_week: z.coerce.number().min(0).max(6).optional(), start_time: z.string().regex(/^\d{2}:\d{2}$/, 'Use HH:MM format'), duration_minutes: z.coerce.number().min(15, 'Minimum 15 minutes').max(240, 'Maximum 4 hours'), room: z.string().optional(), capacity: z.coerce.number().min(1).optional().or(z.literal('')), age_min: z.coerce.number().min(0).optional().or(z.literal('')), age_max: z.coerce.number().min(0).optional().or(z.literal('')), level: z.string().optional() }). Add refinement: if type === 'recurring', day_of_week is required.
    - react-hook-form with zodResolver. Default values from classData in edit mode (useEffect reset on data load).
    - Title: "Add Class" or "Edit Class" (font-display 28px). Below: back link to /admin/classes.
    - Form layout: max-width 600px, fields gap 16px. Same input styling as StudentForm (font-size 16px, height 48px, padding 12px, border var(--color-line), radius var(--radius-sm), font-family var(--font-body)).
    - Fields:
      - Class Name: text input, required
      - Type: native select with options Recurring, Drop-in, Workshop (values: recurring, drop_in, workshop)
      - Instructor: native select populated from useStaff(). First option: "No instructor" (value=""). Then each staff: first_name + ' ' + last_name as label, id as value.
      - Day of Week: native select. Options: Sunday(0) through Saturday(6). Required when type=recurring (show validation error if missing). Hidden or optional when type=drop_in or workshop.
      - Start Time: input type="time" (native browser time picker)
      - Duration: input type="number" with "minutes" label text to the right. Min 15, max 240.
      - Room: text input, optional
      - Capacity: input type="number", placeholder "No limit". Optional.
      - Age Range: two number inputs in a flex row with "to" text between. Labels "Min Age" and "Max Age". Both optional.
      - Level: native select with options: (empty), Beginner, Intermediate, Advanced, Pre-Professional, Open
    - Labels: 14px, font-weight 600, color var(--color-ink-2), margin-bottom 4px. Error messages: 13px red below field.
    - Action buttons: "Save Class" (purple, 48px, 160px min-width), "Cancel" (gray outline, navigates to /admin/classes). In edit mode: "Deactivate Class" (red outline, opens Radix Dialog confirmation "Deactivate {name}? Enrolled students will remain in the roster but no new enrollments will be accepted." Confirm PATCHes active=false, navigates to /admin/classes).
    - On submit: map form data to API body. Convert empty strings to undefined for optional fields. Convert instructor_id empty string to undefined. If creating: useCreateClass().mutateAsync(body). If editing: useUpdateClass(id).mutateAsync(body). On success: navigate('/admin/classes'). On error: show error below form.
  </action>
  <verify>
    <automated>cd /Users/thomasphillips/workspace/LSODance\ Attendance/client && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ClassesPage shows toggle between Calendar and List views
    - WeeklyCalendar renders CSS Grid with 7 day columns and 13 hour rows (7AM-7PM)
    - Class blocks positioned by day_of_week (column) and start_time (row) with correct span
    - Each block shows class name, instructor name, enrollment count/capacity
    - Clicking a block navigates to /admin/classes/:id
    - List view shows all classes as cards with schedule, instructor, enrollment info
    - ClassForm creates/edits with all fields including instructor picker from useStaff
    - Day of week required for recurring classes, optional for drop-in/workshop
    - Deactivation dialog in edit mode
    - All UI meets accessibility: 18px+ body, 56px+ tap targets, Atkinson Hyperlegible
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Fastify API | Class CRUD requires admin role; org scoping from JWT |
| API -> Database | Service role client; org filter on every query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Elevation of Privilege | Class CRUD endpoints | mitigate | request.role !== 'admin' check on every handler |
| T-02-11 | Information Disclosure | GET /classes, GET /staff | mitigate | organization_id filter from JWT on every query |
| T-02-12 | Tampering | POST /classes instructorId | mitigate | Verify instructor (staff) belongs to same org before assigning |
| T-02-SC | Tampering | No new npm packages | accept | This plan installs no new packages. All deps from Phase 1 and Plan 02-01. |
</threat_model>

<verification>
- Server compiles: cd server && npx tsc --noEmit
- Client compiles: cd client && npx tsc --noEmit
- GET /classes returns list with instructor names and enrollment counts
- WeeklyCalendar renders class blocks in correct day/time grid positions
- ClassForm creates a class and it appears on the calendar
- Instructor picker shows active staff members
</verification>

<success_criteria>
- Admin can create a class with name, type, instructor, day/time, duration, room, capacity, age range, and level (CLAS-01)
- Admin can view all classes on a visual weekly calendar with day/time positioning and enrollment counts (CLAS-02)
- Admin can toggle between calendar and list views
- Instructor picker pulls from active staff members
- All class data persists and is org-scoped
</success_criteria>

<output>
Create `.planning/phases/02-studio-management/02-03-SUMMARY.md` when done
</output>
